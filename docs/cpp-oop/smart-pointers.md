---
title: Smart Pointers
---


# Smart Pointers


Overview of `unique_ptr`, `shared_ptr`, and `weak_ptr` with examples. Prefer `unique_ptr` for exclusive ownership and use `shared_ptr` only when necessary. Use `weak_ptr` to break cycles.


### Example run
See the `smart_pointers` folder in the `oops` repo for runnable examples.